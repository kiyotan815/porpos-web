<% provide(:title, '全ユーザー') %>
<div class="container">
  <h1>全ユーザー</h1>
  <ul class="users">
    <% @users.each do |user| %>
      <li>
        <%= link_to user.name, user %>
      </li>
    <% end %>
  </ul>
</div>
